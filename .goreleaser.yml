version: 2

before:
  hooks:
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

archives:
  - id: versioned
    format: tar.gz
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    format_overrides:
      - goos: windows
        format: zip
  - id: latest
    format: tar.gz
    name_template: >-
      {{ .ProjectName }}_{{ .Os }}_{{ .Arch }}
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: 'checksums.txt'

release:
  footer: |
    ---
    **Full Changelog**: https://github.com/polinghasilum4-cpu/lazyfire/blob/main/CHANGELOG.md

changelog:
  # Use CHANGELOG.md content if available, otherwise generate from commits
  use: github-native
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - 'Merge pull request'
      - 'Merge branch'

brews:
  - ids:
      - versioned
    repository:
      owner: xsociety
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    homepage: https://github.com/polinghasilum4-cpu/lazyfire
    description: Terminal UI for browsing Firebase Firestore
    license: MIT
    install: |
      bin.install "lazyfire"
    test: |
      system "#{bin}/lazyfire", "--version"
